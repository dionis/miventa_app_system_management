server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback
  location / {
    try_files $uri /index.html;
  }

  # Cache estático
  location /assets/ {
    expires 30d;
    add_header Cache-Control "public, immutable";
  }
}
