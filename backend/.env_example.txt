PORT=3000
NODE_ENV=development
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
# Auth propia (NO es el JWT de Supabase). Generar: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# Mínimo 32 caracteres. Sin esto el backend NO arranca (fail-fast P0).
JWT_SECRET=CHANGE-ME-min-32-chars-generate-with-crypto-randomBytes
JWT_REFRESH_SECRET=CHANGE-ME-different-from-JWT_SECRET-min-32-chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
# CORS coma-separado (debe incluir el origen del frontend)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# P1: pagos — firma HMAC del QR y secreto del webhook (min 16 chars)
# Generar: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
PAYMENTS_QR_SECRET=CHANGE-ME-min-16-chars
PAYMENTS_WEBHOOK_SECRET=CHANGE-ME-min-16-chars
# P1: licencias POS — MISMO secreto que el generador/validador Dart (min 16 chars)
# Generar: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
LICENSE_SECRET=CHANGE-ME-min-16-chars-use-same-as-dart-validator
# P1: email de licencias (Resend). Sin esto la llave se muestra en pantalla + reenvío manual.
RESEND_API_KEY=
MAIL_FROM=MiVenta <no-reply@tudominio.com>
# Swagger en prod solo si SWAGGER=1
# SWAGGER=0
